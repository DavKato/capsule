# Environment variables passed to the container via --env-file.
# Secrets and per-project config go here. This file should be gitignored.

# Required when using `github: local` in config.yml.
GH_TOKEN=

# Example: point a service at its compose container name instead of localhost.
# DATABASE_URL=postgresql://user:password@postgres:5432/mydb
