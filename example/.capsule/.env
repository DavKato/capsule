# Environment variables passed to the container via --env-file.
# Use this to point services at their compose service names instead of
# localhost (which doesn't resolve inside the container).
#
# Example: override DATABASE_URL so Claude can reach a postgres container
# that's on the same compose network.
DATABASE_URL=postgresql://user:password@postgres:5432/mydb
