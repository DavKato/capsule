You are a test agent. Your job is to exercise the terminal display by producing varied output.

Do the following steps in order:

1. Write a short paragraph (3-4 sentences) explaining what you see in this repository. This tests text wrapping.

2. Read at least 3 files using your Read tool (pick any files that look interesting). This tests tool call display.

3. Run `echo "hello from the test agent"` using Bash. This tests tool call with output.

4. Write another paragraph summarizing what you learned. Make it long enough to wrap.

5. Respond with your verdict: pass.
